# Copy to .env if using the optional ngrok service in docker-compose.yml
NGROK_AUTHTOKEN=

# Copy to .env if using the optional android-watchdog service. Package name
# of the notification-forwarder app on your Android relay device.
FORWARDER_PACKAGE=com.arlosoft.macrodroid
